# Trabalho G1 – Front-End

**Nomes:** Lucas Varal e Nycolas C. Jungbeck
**RA:** 1136676, 1139527
**Disciplina:** Front-End — Prof. Matheus Henrique Barquette

## Referência

Site utilizado como referência: [Página de login do GitHub](https://github.com/login)

## Checklist da Parte 1

- [x] **1.1 – Estrutura HTML semântica e acessível:** a página usa `header`, `main`, `section` e `footer`. O formulário de login tem `label` associado a cada campo via `for`/`id`, e a imagem do logo possui atributo `alt` descritivo.
- [x] **1.2 – Fidelidade visual à referência:** reproduzidas as proporções, cores e organização geral (header simples, card central de login, footer com links) da tela de login do GitHub.
- [x] **1.3 – CSS: seletores, box model e variáveis:** uso de seletores de classe (`.login-card`), descendente (`.login-form input`) e pseudo-classe (`:hover`). Variáveis CSS definidas em `:root` para cores e espaçamentos.
- [x] **1.4 – Responsividade:** CSS escrito mobile first, com Flexbox para organizar os elementos, e uma media query `min-width: 768px` ajustando o layout para desktop.
- [x] **1.5 – Personalização e originalidade:** seção "Sobre este clone", que não existe na página original, foi adicionada explicando o contexto acadêmico do projeto.

